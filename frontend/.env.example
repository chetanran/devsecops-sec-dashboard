# Azure AD Configuration
# Get these values from Azure Portal after creating App Registration

# Your Azure AD Tenant ID (Directory ID)
# Found in: Azure Portal > Azure Active Directory > Overview
REACT_APP_AZURE_TENANT_ID=your-tenant-id-here

# Your Application (Client) ID
# Found in: Azure Portal > App Registrations > Your App > Overview
REACT_APP_AZURE_CLIENT_ID=your-client-id-here

# Redirect URI (should match what's configured in Azure AD)
# For local development, use http://localhost:3000
# For production, use your deployed frontend URL
REACT_APP_REDIRECT_URI=http://localhost:3000

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Azure AD credentials
# 3. Never commit the .env file to version control
# 4. Make sure the redirect URI matches what's configured in Azure Portal
